language: ruby
dist: trusty
sudo: false
rvm:
- 2.3.1
jdk:
  - oraclejdk8
  - oraclejdk8

before_install:
  - gem bundler

install:
  - bundle install

script:
  - bundle exec rake
  - htmlproofer ./_site
