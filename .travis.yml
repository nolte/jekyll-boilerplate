language: ruby
dist: trusty
sudo: true
rvm:
- 2.3.1
jdk:
  - openjdk8
  - oraclejdk8

before_install:
  - gem install bundler
  - sudo update-ca-certificates

install:
  - bundle install

script:
  - bundle exec rake build
  - bundle exec rake html_proofer
