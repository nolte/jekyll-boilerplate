language: ruby
dist: trusty
sudo: false
rvm:
- 2.3.1
jdk:
  - openjdk8
  - oraclejdk8

env:
  global:
    - NOKOGIRI_USE_SYSTEM_LIBRARIES=true

before_install:
  - gem install bundler

install:
  - bundle install

script:
  - bundle exec rake build
  - bundle exec rake html_proofer
