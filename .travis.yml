language: ruby
dist: trusty
sudo: false
rvm:
- 2.3.1
jdk:
  - openjdk8
  - oraclejdk8

before_install:
  - gem install bundler

install:
  - bundle install

script:
  - bundle exec rake build
  - bundle exec rake html_proofer

notifications:
  webhooks:
    urls:
      - http://lit-scrubland-83566.herokuapp.com/travis
